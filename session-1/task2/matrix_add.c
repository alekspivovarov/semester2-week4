
/*
 * Sum of 2 matrices
 */

 #include <stdio.h>

 int main( void ) {
    float a[4][4];
    float b[4][4];
    float c[4][4];

    /*
    Write code to compute the sum.
    Intialise the matrix a and b entries to 1. 
    Store your answer in matrix c
    Print your final answer
    */

   //Initialise entries of both a and b matrices to 1
   for (int i = 0; j < 4; i++) {
      for (int j = 0; j < 4; j++) {
         a[i][j] = 1.0;
         b[i][j] = 1.0
      }
   }

    return 0;
 }
